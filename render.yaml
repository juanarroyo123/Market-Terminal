services:
  - type: web
    name: market-intelligence
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: POLYGON_KEY
        sync: false   # Rellena esto en el dashboard de Render si tienes API key de Polygon
